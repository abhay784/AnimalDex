//! Media service — presigned uploads and thumbnailing.
//!
//! Scaffolded; implementation lands next. Split from `core-api` because image
//! work is CPU-bound with completely different scaling characteristics from
//! JSON CRUD, and because photos should never proxy through the API at all.

fn main() {
    println!("media-svc: not yet implemented");
}
